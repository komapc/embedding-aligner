# Core dependencies for embedding alignment project

# Deep learning / BERT pipeline (steps 13-17).
# Pin transformers to 4.x: 5.x removed TrainingArguments.overwrite_output_dir
# and the Trainer `tokenizer=` arg these scripts rely on. >=4.46 keeps the
# `eval_strategy` name used by the cross-encoder trainer.
transformers>=4.46,<5
datasets>=2.14.0
accelerate>=0.26.0
sentencepiece>=0.1.99
torch>=2.0.0

# Word embeddings
gensim>=4.3.0

# Numerical computing
numpy>=1.24.0
scipy>=1.10.0

# Machine learning utilities
scikit-learn>=1.3.0

# Progress bars
tqdm>=4.65.0

# Data handling
pandas>=2.0.0
